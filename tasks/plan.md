# 统一实施计划：五分钟暴兵、波次构筑与金币商店

## 1. 目标与当前基线

本计划取代此前的经验曲线方案，并继续承接已经完成的战斗反馈、移动相机、升级文字与工业硬核 BGM。最终体验是一个约五分钟的高密度割草闭环：传送门在玩家周围安全环带内集中暴兵，清空一波后进入统一结算，玩家先免费领取一次流派奖励，再用本波收集的金币购买补强；手动暂停只负责继续游戏。连杀超载提供短时无敌和超高输出。

已经完成并保留：

- 单一伤害/击杀事实、受限 hit-stop、击杀确认、镜头反馈。
- 稳定移动与相机跟随、冲刺基础、升级选项中文可读性。
- 168 BPM 工业硬核 BGM 与基础音量控制。
- 自动化测试入口：`powershell -ExecutionPolicy Bypass -File scripts/tests/run_tests.ps1`。

## 2. 核心循环

```text
传送门预警 -> 多门暴兵 -> 高速清场 -> 金币散落与吸附拾取
      -> 清剿完成提示 -> 三流派各两卡统一结算
      -> 免费领取一张 -> 可继续付费购买 -> 关闭并进入下一波
      -> 连杀充能 -> 短时无敌超高输出 -> 最终首领/结算
```

成长通过同一结算界面提供两个互补通道：

- 波次升级：免费、每个可继续作战的已清波次一次，主要决定弹幕、无人机/电弧、冲刺/地刺三条流派及终极进化。
- 金币购买：领取免费奖励后消费本局金币，主要提供可控补强、缺口修复和流派协同；结算快照稳定，不能刷新报价。

终局波清空后直接结算，不弹出没有后续战斗价值的升级选择。

## 3. 产品验收指标

- 单局中位时长目标 `5:00`，正常范围 `4:30–5:30`。
- 玩家在前两次波次升级内看到明确流派方向，终局前获得一次终极进化。
- 每个非终局波次清空后恰好获得一次免费三选一；重复事件不能重复奖励。
- 敌人不再提供经验；死亡只生成一次金币掉落和既有护盾掉落。
- 金币拾取只结算一次，本局余额不能为负；统一结算重复打开不能生成新报价或免费刷新。
- 每次攻势在玩家周围安全环带生成多个传送门，主要敌群在短时间内集中涌出。
- 超载持续时间短，完整窗口无敌且高输出，音画起止清晰，退出后临时倍率完全恢复。
- 阶段快照包含金币、升级、商店报价状态和待开始阶段；不保存屏幕中的敌人、弹幕、掉落与 VFX。

## 4. 架构决策

### 4.1 单一击杀收益事实

`Enemy` 只携带 `coin_value`；`WaveDirector` 只发出一次 `enemy_killed(enemy, source, coin_value)`，并延迟生成一次金币掉落。`Main` 负责把拾取结果交给 `UpgradeSystem`，表现层不得自行增加余额。

### 4.2 成长状态归属

第一版继续由 `UpgradeSystem` 持有本局成长状态，避免在核心链路未稳定前拆出空泛框架：

```gdscript
signal progression_state_changed(state: Dictionary)
func add_coins(amount: int) -> bool
func spend_coins(amount: int) -> bool
func prepare_settlement(completed_wave: int) -> bool
func claim_free_offer(request: Dictionary) -> bool
func purchase_settlement_offer(request: Dictionary) -> bool
func complete_settlement(request: Dictionary) -> bool
```

`family_levels` 分别表示火力、机动、工程等级，初始均为一；只有免费奖励提升对应流派等级。每次结算使用不可伪造、不可重复消费的事务号，付费购买只增加卡牌层数。

### 4.3 波次推进所有权

`WaveDirector` 在清空波次后进入 `waiting_for_advance`，只发一次 `wave_finished`。`Main` 依次控制清剿提示和统一结算；合法关闭结算后显式调用 `advance_after_settlement()`，再由下一次 `wave_prepared` 打开开场提示。这样下一波不会在提示或选择界面背后提前生成。

### 4.4 商店报价稳定性

统一结算为每个流派生成两张稳定报价，重复打开不刷新，已领取/购买项标记售罄。报价来自同一卡牌目录，并携带 `family`、`kind`、`cost`、`max_rank` 与事务号。领取免费奖励后才允许继续付费购买或关闭；手动暂停不再提供报价入口。

### 4.5 存档边界

只在稳定边界保存：免费奖励完成后、结算购买完成后、准备进入下一阶段时。快照保存可重建数据，不保存运行时节点。损坏或未知版本安全回退到新游戏。

## 5. 分阶段任务

### Phase 1：波次升级与金币基础闭环

#### Task 1.1：替换经验成长契约

验收：

- `UpgradeSystem` 不再暴露经验阈值；金币加减安全，余额不为负。
- 每个有效非终局波次只能排队一次免费升级；应用升级后等级增加一次。
- 伪造、重复或封顶选项继续被拒绝。

验证：`UpgradeTest`、`BalanceTest`。

#### Task 1.2：建立可暂停的波次结算门

验收：

- 波次清空只发一次 `wave_cleared`，等待升级期间不启动下一波。
- 升级完成后显式推进；最终波直接胜利且不留下待选升级。
- 现有生成帧率独立性、敌人注册表与胜利路径不回归。

验证：`WaveTest`、`StateTest`、`SmokeTest`。

#### Task 1.3：金币掉落、拾取与 HUD

验收：

- 所有 `xp_value`/`ExperienceShard` 运行时路径替换为金币语义。
- 单次击杀只生成一枚对应价值的金币掉落，单枚掉落只结算一次。
- HUD 显示“流派等级”和“金币”，不再显示经验进度条。

验证：`CombatEventTest`、`ProjectilePickupTest`、`UITest`、`PerformanceTest`。

### Checkpoint A：第一阶段

- 完整测试通过，无 XP 运行时引用。
- 手动确认金币可见、可吸附、余额变化；清空波次后升级界面出现且选择后进入下一波。
- 第一阶段形成聚焦提交并推送。

### Phase 2：暂停商店闭环（历史基线，Phase 3 已统一替换）

#### Task 2.1：共享升级目录与商店报价契约

- 为升级补齐流派、核心/支援类型、费用、前置和封顶信息。
- 每波稳定生成三项报价；暂停切换不重抽，购买后售罄。
- 费用随阶段温和增长，余额不足或非法事务不改变状态。

#### Task 2.2：暂停商店 UI

- 暂停界面同时提供继续按钮、余额和三项可购买升级。
- 明确显示费用、流派、说明、售罄/余额不足状态，键盘与鼠标均可操作。
- 购买后立即更新玩家属性、余额、报价和反馈。

#### Task 2.3：商店状态集成

- 暂停所有权仍只由 `Main` 状态机管理。
- 购买不会关闭暂停界面；恢复后临时战斗状态保持干净。
- 商店购买与免费波次升级共享效果实现，但使用独立事务和统计。

### Checkpoint B：双通道成长历史基线

- 免费波次升级与金币购买都能稳定改变构筑，互不重复结算。
- 反复暂停不能刷报价或增加金币。
- 960×540 至 2560×1080 均可读可操作。

### Phase 3：统一结算、三流派构筑与战斗重量

详细合同与数值表见 `docs/specs/2026-07-19-phase3-settlement-builds-combat.md`。

#### Task 3.1：战斗反馈重量分级

- 轻型击杀使用小震屏；中型保持当前力度；大型使用完整震屏。
- 枪声替换电子提示音；轻/中/大型受击音按重量区分。
- Bruiser 基础生命调整到 `300`，但接触伤害不同时提高。

#### Task 3.2：远程敌人可见交战区

- Spitter 出生在相机安全区与玩家距离环的交集内。
- 离开可见安全区时全速返回；靠边且玩家过近时沿边横移，不向屏幕外逃。
- 未进入可见交战区前不能开火。

#### Task 3.3：波次提示与显式推进

- 状态机按 `WAVE_INTRO -> PLAYING -> WAVE_CLEAR -> SETTLEMENT` 推进。
- 开始提示结束前不生成敌人；完成提示结束前不显示结算。
- 最终波也显示完成提示，但之后直接进入胜利结算。

#### Task 3.4：三流派六卡统一结算

- 火力、机动、工程每个流派稳定生成两张卡。
- 先免费领取一张，之后才能付费购买和关闭；三个动作使用独立防伪事务。
- 手动暂停不再提供第二条购买入口；点击关闭才允许下一波开始。

#### Task 3.5：流派等级与卡池重平衡

- 三流派等级初始均为 `1`；免费奖励提升对应等级，付费只升卡牌层数。
- 普通卡单层收益控制在 `8%–20%`，射速、移速、地刺寿命、无人机数等均有硬上限。
- 地刺与移速/冲刺、无人机与电弧、枪线与弹速/穿透形成可读联动。

#### Task 3.6：三条终极进化

- 火力：轨道风暴。
- 工程：雷网矩阵。
- 机动：裂地超载。
- 第四波后且对应流派等级达到 `5` 才进入候选池；每局最多一个终极进化。

### Checkpoint C：构筑深度

- 四种目标分辨率下完整通过“提示、战斗、提示、免费、购买、关闭、下一波”。
- 三条路线在相同免费投资下输出量级接近，并在前两次结算形成明确方向。
- 每波通常可多买一张，存钱可买两张，不能稳定买空剩余五张。
- 轻重敌人反馈、Bruiser TTK 与 Spitter 可见性达到规格中的自动化边界。

### Phase 4：传送门暴兵与连杀超载

#### Task 4.1：传送门组件与安全环采样

- 传送门状态为预警、暴兵、关闭；硬安全距离不低于 `260px`。
- 边界回退仍满足安全距离，单帧生成受预算限制。

已完成：程序化霓虹传送门组件提供“预警 → 暴兵 → 关闭”生命周期；`WaveDirector` 安全环采样保证世界边界与 `260px` 最小距离。敌人从门内分批涌出将在 Task 4.2 接入。

#### Task 4.2：多门集中攻势

- 每次攻势生成多个门并在短窗口集中涌敌，不再全地图零散刷新。
- 活跃敌人、队列和门均清空后才允许波次结算。

#### Task 4.3：连杀超载状态

- 连杀充能、短时触发、全程无敌、全伤害高倍率和高射速倍率有单一状态源。
- 退出、暂停、升级、结算与重启后所有临时倍率归零。

#### Task 4.4：超载音画与 HUD

- 触发重击、短循环、结束尾音与角色高亮、边缘能量、受限震屏明确区分起止。
- 高密度输出下不产生无限音频播放器、VFX 节点或常驻 hit-stop。

### Checkpoint D：爽感核心

- 暴兵具有集中压迫感但保留公平预警。
- 超载短、稀有、压倒性，且玩家能明确理解无敌和高输出窗口。

### Phase 5：六阶段、首领与继续游戏

#### Task 5.1：六阶段节奏、远程敌人扩展与最终首领

- 用六个阶段替换旧八波数量表；第六阶段清场后进入最终首领，首领具有预警攻击和传送门援军。
- 在 Phase 4 传送门和生成预算稳定后加入 Marksman 与投射型远程敌人；保留 Spitter 作为中距离基础远程单位。
- 标准构筑中位时长进入 `4:30–5:30`。

#### Task 5.2：版本化阶段快照

- 保存金币、等级、升级计数、进化、稳定商店报价、玩家基础状态、击杀和用时。
- 原子写入，拒绝损坏、非法数值和未知版本。

#### Task 5.3：继续游戏流程

- 有效快照提供继续入口，从待开始阶段重建。
- 胜利、失败和主动新游戏清理旧档；中途退出保留最近稳定快照。

### Checkpoint E：五分钟闭环

- 新游戏、暂停购买、波次升级、保存、继续、胜负与重开形成完整闭环。
- 不恢复敌人、弹幕、门、掉落、VFX、音频循环或相机偏移。

### Phase 6：平衡、性能与最终验收

#### Task 6.1：经济与构筑平衡

- 记录各阶段金币收入、购买率、首次成型时间、进化达成率和三条路线通关率。
- 商店价格让玩家每阶段通常能做一项有意义取舍，而不是买空或完全买不起。

#### Task 6.2：密集战斗性能

- 250 敌人、最大传送门攻势与最高弹幕不产生无上限节点、RID 或音频播放器。
- 30/60/120 Hz 下关键计时与生成数量保持容差。

#### Task 6.3：可访问性与发布回归

- HUD、统一结算、纯暂停页和结果页在四种目标分辨率下可读。
- 震屏/闪光强度只影响表现，不影响规则；Windows 导出启动通过。

## 6. 依赖与并行边界

```text
Phase 1 成长/波次/金币契约
  -> Phase 2 暂停商店
  -> Phase 3 流派与进化
  -> Phase 5 存档数据格式

Phase 1 波次结算门
  -> Phase 4 传送门攻势
  -> Phase 5 六阶段/首领

既有击杀事实 + 有效属性/免疫合同
  -> Phase 4 连杀超载
```

Phase 2 的商店 UI 可在报价合同锁定后与目录元数据并行；Phase 4 的传送门线和超载线可在各自不同时修改 `Main.gd` 的前提下并行。涉及 `Main.gd`、`WaveDirector.gd`、`UpgradeSystem.gd`、`Player.gd` 的集成任务必须按依赖顺序合并。

## 7. 风险与控制

| 风险 | 影响 | 控制措施 |
|---|---|---|
| 波次清空重复发奖 | 高 | `waiting_for_advance` + 已奖励波次去重 + 重复事件测试 |
| 旧 XP 与金币双重结算 | 高 | 删除 XP 运行时路径；单一击杀收益事实；全仓检索门禁 |
| 暂停刷商店报价 | 高 | 报价按波次持久化；暂停/恢复只展示，不重新生成 |
| 金币过多导致商店无取舍 | 中 | 记录阶段收入分位数；成本随阶段增长；购买率回归测试 |
| 商店稀释流派方向 | 中 | 免费升级偏核心，商店偏支援；家族权重与里程碑保底 |
| 升级暂停背后提前开波 | 高 | `WaveDirector` 显式等待，只有完成升级才推进 |
| 超载临时倍率污染永久属性 | 高 | 基础值/有效值分离；状态退出和重启回归 |
| 暴兵同帧卡顿 | 高 | 门级与全局生成预算、活跃敌人上限、性能基线 |

## 8. 完成标准

- 所有阶段验收标准与检查点通过，任务清单状态同步。
- 每项新行为先有失败测试，再完成实现；完整测试无错误、泄漏或跳过。
- 每个纵向切片形成聚焦提交并推送，不包含凭据、生成状态或无关改动。
- 三条构筑、金币商店、传送门暴兵、超载、保存继续和五分钟时长完成手动回归。

## 9. 2026-07-26 反馈修正阶段：战场节奏与 Boss 可读性

本阶段优先修复实际试玩暴露出的五项体验问题，完成后进入人工 Boss 战试玩。预计工程时间 **4–7 小时**，人工验收 **30–60 分钟**；推荐继续使用当前高推理编码模型完成跨系统改动，试玩反馈收集不需要转换模型。

### Task 9.1：Boss HUD 与登场演出

- Boss 血条作为独立覆盖层，水平居中并位于玩家状态 UI 下方；显示/隐藏不得改变 HUD 位置。
- 最终 Boss 出场包含约 1.4 秒独立演出：战斗逻辑暂停、界面与演出继续更新，结束后才允许 Boss 攻击。
- 四种目标分辨率下血条不越界、不挤压玩家状态卡。

### Task 9.2：Boss 近远程分区

- 近距离只调度有明确预警的触手/近战攻击，并禁止贴脸环形弹幕。
- 中远距离才使用扇形、螺旋和缺口环形弹幕；Boss 主动维持可读交战距离并回到相机安全区。
- 任一调度窗口内不叠加不可躲的近战与主弹幕。

### Task 9.3：传送门逐个生成与冲散

- 每个门每 `0.2s` 只在中心生成一个敌人；门的生成阶段持续时间严格为 `生成数 × 0.2s`。
- 新生敌人获得随机方向初速度，并在短冲刺窗口内优先执行该速度，避免出生重叠。
- 门位于玩家约半个屏幕距离的随机角度；同批门满足最小间距，采样失败时使用确定性错位回退。

### Task 9.4：5 秒拾取窗口与升级过渡

- 普通阶段清空后进入 `5.0s` 非暂停拾取窗口：玩家、金币和表现继续更新，但不再生成敌人。
- HUD 显示平滑倒计时提示；窗口结束后才进入完成提示与升级界面。
- 最终阶段同样保留拾取时间，再进入最终结算/首领门禁。

### 验证与依赖

```text
HUD 独立布局 ───────────────┐
Boss 登场门禁 -> 近远程分区 ├─> Boss 人工试玩
传送门节奏 -> 敌人出生冲散 ─┤
波次清空 -> 5 秒拾取窗口 ───┘
```

每项行为先补失败测试，再实现；专项测试通过后运行完整套件两次。提交只包含本阶段相关文件，不包含 `project.godot` 的本地差异和 Godot 自动生成的 `.uid` 文件。

## 10. 2026-07-26 二次试玩反馈：Boss 信息与阶段收尾

本轮在现有 Boss 与传送门节奏基础上做一次聚焦修正，预计工程时间 **1.5–2.5 小时**，完成后进入人工 Boss 战试玩。

### Task 10.1：Boss 生命值与居中入场

- Boss 血条叠加精确生命数值；只要实际生命仍大于零，显示值至少为 `1`，实际归零时才显示 `0`。
- Boss 入场演出使用屏幕空间覆盖层，演出中心严格跟随视口中心；演出结束后才显示世界中的 Boss 并恢复战斗。
- 四种目标分辨率下验证演出中心、血条数值与显隐状态。

### Task 10.2：传送门冲散与阶段收尾

- 保持每 `0.2s` 出怪节奏不变，仅把怪物出生初速度从 `280` 降至 `140`，持续时间不变。
- 普通阶段继续保留 `5.0s` 金币拾取窗口，HUD 只显示 `倒计时：...s`。
- Boss 战结束跳过金币拾取窗口，直接进入最终阶段完成流程。

### Checkpoint G：二次人工试玩

- 专项测试与完整测试连续通过，代码审查无阻断问题。
- 聚焦提交并推送后发送邮件，直接启动当前工作树中的游戏供人工试玩。

## 11. 2026-07-26 Phase 6.1：经济与三流派构筑平衡

本阶段用可重复的真实结算事务样本替代主观估算，预计 **2–4 小时**。模拟直接读取正式波表、敌人金币、卡池、价格曲线和升级效果，不复制一份易漂移的平衡数据。

### Task 11.1：经济样本基线

- 为火力、机动、工程三条聚焦路线分别运行固定种子的多局样本。
- 记录每阶段理论金币、结算余额、付费购买数和存钱后双购机会。
- 验收：常规策略每阶段通常能买一张，至少出现一次可选择的双购机会，且不能稳定买空五张。

### Task 11.2：构筑成型与终局输出

- 记录第二次结算后的核心机制/路线辨识度、首次流派等级 5、终极进化达成阶段。
- 以正式 Player 属性和伤害倍率计算三路线终局持续输出代理值与 Boss TTK 代理值。
- 验收：三路线均在 Boss 前成型并获得对应进化；相同投资下输出代理值保持同一量级，不出现明显失效路线。

### Task 11.3：报告与回归门禁

- 生成一份包含样本规模、假设、分阶段经济分位数、三路线结果和调参结论的 Markdown 报告。
- 将经济/构筑样本加入自动化测试入口，后续修改波表、金币、价格或升级效果时自动阻止失衡回归。
- 专项测试通过后运行完整回归、代码审查、聚焦提交并推送。

### Checkpoint H：Phase 6.1 完成

- 自动样本和完整回归通过；如果数据需要改变核心经济目标或路线定位，则在调参前请求人工决策。
- 无需重大决策时完成本阶段并继续到 Phase 6.2；到需要人工性能/手感验收时发送邮件并启动游戏。

## 12. 2026-07-26 Phase 6.2：密集战斗性能与资源回收

本阶段先测量现有高密度路径，再只优化有证据的热点。自动验收覆盖 250 敌人、五个并行传送门、Boss 最高弹幕、战斗 VFX 与音频池，预计 **1.5–3 小时**。

### Task 12.1：可重复压力基线

- 在 30/60/120 Hz 下模拟同一批传送门生成，验证最终数量、逐门 `0.2s` 节奏和清场结果一致。
- 记录 250 敌人注册表查询、传送门生成和四无人机索敌耗时；时间数据用于对比，不使用依赖机器性能的脆弱硬阈值。
- 压满战斗 VFX、Boss 弹幕与音频提示，验证公开容量限制不会被突破。

### Task 12.2：热点优化与泄漏门禁

- 避免同一帧的重复敌人列表复制或全树扫描，并用调用次数和等价结果固定优化行为。
- Boss 弹幕自然销毁后必须同步移出控制器追踪表；传送门异常离树后必须清理队列与计时器。
- 压力夹具释放后，节点、弹幕追踪、门队列和 VFX 记录回到基线，不保留无界增长容器。

### Task 12.3：报告与阶段验收

- 生成包含测量环境、前后数据、容量上限和剩余风险的性能报告。
- 专项测试通过后运行完整回归与五轴审查，形成聚焦提交并推送。
- 无需人工判断时继续 Phase 6.3；到 Windows 导出/视觉可读性人工验收点才发送邮件并启动游戏。

## 13. 2026-07-26 Phase 6.3：可访问性与 Windows 发布回归

本阶段完成可自动验证的发布门禁，再停在必须人工观察与试玩的检查点。预计自动工程时间 **1.5–3 小时**，人工验收 **0.5–1 小时**。

### Task 13.1：四分辨率 UI 与输入可达性

- 在 `960×540`、`1280×720`、`1920×1080`、`2560×1080` 下覆盖开始页、HUD、Boss 条/入场、统一结算、暂停页和结果页的尺寸契约。
- 模态层继续拦截鼠标；打开时聚焦首个有效操作，关闭后焦点回到 HUD；键盘快捷键不得绕过禁用操作。
- 中文字体回退、最小正文尺寸和关键文字对比度保持在主题基线之上。

### Task 13.2：表现与规则隔离

- 震屏与闪光只修改相机/VFX 表现状态，不写入玩家、敌人、波次、伤害或计时规则。
- 表现重置后相机偏移、旋转和临时 VFX 全部归零，不污染继续游戏快照。

### Task 13.3：Windows 发布门禁

- 提供版本化的 Windows Desktop 导出预设；在缺少大型官方模板时至少可生成 PCK 并用当前 Godot 运行时启动冒烟。
- 正式独立 EXE 导出模板若未安装，明确记录为人工发布前置条件，不把网络下载隐式加入工程。
- 自动回归、发布冒烟、diff 审查通过后提交推送，发送邮件并直接启动当前游戏进入人工视觉/Boss 战验收。

## 14. 2026-07-26 三次试玩反馈：Boss 节奏、掉落手感与流派再平衡

本阶段先收敛人工试玩暴露的问题，再直接进入 Phase 6.5 发布终审。预计工程时间 **3–5 小时**，完成后需要约 **30–60 分钟**人工试玩确认手感。

### Task 14.1：Boss UI 与近战节奏

- Boss 血条按视口像素宽度计算，严格水平居中并保持在玩家状态 UI 下方，不依赖含糊的锚点偏移。
- 缩小近战横扫角度，延长横扫/砸击预警，并在每次攻击完成后加入真实恢复间隔，避免攻击动画期间提前耗尽冷却。

### Task 14.2：掉落物与地刺层级

- 金币不再依赖拾取范围；掉落后自动追向玩家，速度随存活时间连续增加并设置稳定上限。
- 护盾掉落不受磁吸影响，保持原地等待玩家接触。
- 地刺动画绘制在敌人下方，命中规则不受视觉层级影响。

### Task 14.3：三流派数值与机制再平衡

- 删除所有拾取范围卡，分别替换为地刺范围/频率和电弧频率机制卡。
- 火力维持单体与穿透/多线之间的明确取舍；机动把更多强度转移给地刺；工程强化电弧伤害、半径和脉冲频率。
- 固定种子样本重新验收三路线输出量级、Boss TTK、核心机制贡献占比和成型阶段。

### Task 14.4：Phase 6.5 发布终审

- 专项测试通过后运行完整回归两次、Windows PCK 冒烟和五轴代码审查。
- 聚焦提交并推送；邮件提醒后直接启动当前工作树版本，进入最终人工试玩。

## 15. 2026-07-26 四次试玩反馈：雷网终极效果与全武器过载

本阶段把雷网矩阵从纯数值增量改为可见、可验证的终极机制，并让过载对地刺、无人机、电弧、移动和冲刺使用同一个临时状态契约。预计工程时间 **3–5 小时**，完成后进入人工终局/过载试玩。

### Task 15.1：雷网矩阵与无人机索敌

- 雷网矩阵使电弧覆盖当前屏幕，无人机激光改为紫色且伤害为常态的两倍。
- 无人机优先选择尚未被其他无人机占用的目标；仅当没有未占用目标时，才回退到最近目标，保证单 Boss 时所有无人机持续输出。
- 每帧只读取一次敌人注册表，不能因两阶段索敌增加场景树或注册表扫描次数。

### Task 15.2：过载派生属性契约

- 过载生成的地刺半径 `2×`、伤害 `2×`、持续时间 `1.5×`；无人机激光宽度 `1.5×`、伤害 `2×`。
- 电弧半径 `2×`、伤害 `3×`、频率 `1.5×`；移速 `1.3×`；冲刺冷却减少 `30%`。
- 所有倍率来自只读有效值/来源修饰器，退出过载后基础属性不被污染；进入结算升级或暂停商店时立即退出过载。

### Task 15.3：阶段收尾与金币节奏

- 普通阶段金币拾取窗口从 `5.0s` 缩短为 `3.0s`，Boss 战仍直接进入最终流程。
- 金币初速、加速度和速度上限整体提高 `2×`，继续全图自动追踪且不增加每帧目标查找。

### Task 15.4：裂地终极地刺与 Boss 受击反馈

- 裂地超载激活后，冲刺路径生成独立的黑色地刺，基础伤害为普通地刺的 `3×`；步行路径继续生成原有粉色地刺。
- 普通地刺和无人机激光命中 Boss 时保留命中音效但禁止受击抖动；黑色冲刺地刺命中 Boss 时保留重击抖动。
- 黑色地刺继续复用地刺碰撞、图层和过载倍率契约，避免出现第二套持续伤害实现。

### Task 15.5：Boss 前最终结算

- 第六阶段普通敌人清空后先进入 `3.0s` 金币回收窗口，再开放一次完整的免费升级与购买结算。
- 关闭该结算后才触发 Boss 传送门和入场动画；Boss 击败后仍跳过金币窗口并直接完成最终流程。
- 为 Boss 前结算增加可恢复快照边界，继续游戏不能跳过购买或重复生成奖励。

### Task 15.6：验证与试玩交接

- 专项测试覆盖终极技能、索敌回退、过载进入/退出、升级页清理、三秒窗口、金币速度、黑色地刺反馈与 Boss 前结算恢复。
- 完整回归、性能门禁、Windows PCK 冒烟和五轴代码审查通过后，聚焦提交推送、发送邮件并启动当前新版。

完成记录：28 套自动化测试连续两轮通过（每轮 88,791 条断言），Windows PCK 导出与 120 帧启动冒烟通过；下一检查点为人工试玩终极技能、过载手感与 Boss 前结算节奏。

## 16. 2026-07-27 五次试玩反馈：地刺可见性、电弧波前与构筑前置

本阶段修复终极技能试玩暴露的表现和成长语义问题。预计工程时间 **2–4 小时**，完成自动回归与发布冒烟后进入人工试玩。

### Task 16.1：世界绘制层级与过载倍率

- 地板、地刺、敌人使用明确的递增绘制层级，保证地刺不会被地板遮挡，同时仍绘制在敌人下方。
- 过载期间电弧伤害从 `3×` 调整为 `1.5×`；退出过载后仍恢复基础倍率。

### Task 16.2：雷网矩阵扩散波前

- 雷网矩阵继续覆盖全屏，但扩散速度降低为普通电弧原速度的 `30%`。
- 发射时不再瞬间伤害范围内全部敌人；电弧波前扫过敌人时才结算一次伤害，同一波电弧对同一敌人最多命中一次。

### Task 16.3：无人机数量与卡片前置

- 雷网矩阵不再额外生成无人机；每次“无人机部署”只增加一架无人机。
- 地刺、无人机、电弧的派生卡在对应启动卡尚未获得前保持锁定；工程流派的两个启动器在未取得时优先进入候选，同时雷网矩阵要求两项核心机制均已启动。

### Task 16.4：验证与试玩交接

- 专项回归覆盖绘制顺序、电弧倍率、慢速波前单次触碰伤害、无人机精确数量和卡片前置。
- 完整回归、Windows PCK 冒烟与差异审查通过后，聚焦提交推送、发送邮件并直接启动当前工作树版本。

完成记录：28 套自动化测试连续两轮通过（每轮 88,811 条断言）；Windows PCK 导出为 291,180 字节，主场景 120 帧发布冒烟通过。下一检查点为人工试玩地刺可见性、雷网波前命中节奏和卡池解锁顺序。

## 17. 2026-07-28 六次试玩反馈：瞄准镜、距离衰减与出生稳定性

本阶段强化瞄准可读性，并收紧电弧与地刺的基础强度，同时修复新局出生帧可能继承输入而位移的问题。预计工程时间 **2–3 小时**，完成后进入人工手感验收。

### Task 17.1：战斗瞄准镜与玩家尺寸

- 新增跟随鼠标的高对比青色/洋红双环十字瞄准镜，保持鼠标事件穿透，只在实际战斗状态显示。
- 玩家画面、碰撞半径、世界边界余量和子弹出生点统一放大 `30%`，不改变无人机轨道与技能范围。

### Task 17.2：电弧衰减与扩散速度

- 电弧原始伤害从 `34 + 12×等级` 调整为 `17 + 6×等级`，确保所有基础伤害减半。
- 单次波前命中伤害按敌人与玩家距离线性衰减：中心为 `100%`，最大半径边缘保底 `25%`。
- 普通电弧和雷网矩阵的扩散速度统一为旧普通电弧的 `50%`；雷网全屏范围不变。

### Task 17.3：地刺密度与出生输入门禁

- 初始地刺间距从 `48px` 增加到 `96px`，即初始放置密度降低 `50%`，后续密度升级仍可缩短间距。
- 玩家创建后到正式进入 `PLAYING` 前关闭物理处理；进入战斗后先等待一次移动/冲刺输入完全松开，再允许位移。
- 曾尝试通过独立碰撞层阻止敌人实体挤压，但试玩确认该方案同时切断护盾拾取、敌方子弹命中、普通敌人与 Boss 身体阻挡，属于严重回归，现已完整撤销；出生稳定性不得通过关闭玩家碰撞解决。
- 进入 `PLAYING` 时立即将相机平滑状态同步到玩家最终出生/读档坐标，避免相机从旧位置追赶造成“玩家自行滑动”的视觉假象。

### Task 17.4：验证与试玩交接

- 专项回归覆盖瞄准镜尺寸/跟随/模态隐藏、电弧基础伤害与距离衰减、地刺间距、玩家尺寸和出生输入门禁。
- 完整回归、Windows PCK 冒烟和差异审查通过后，聚焦提交推送、发送邮件并直接启动当前工作树版本。

初次完成记录：28 套自动化测试通过（88,823 条断言）；Windows PCK 导出为 295,656 字节，主场景 120 帧发布冒烟通过。试玩发现出生稳定性仍未解决，后续回归补充了零输入碰撞恢复与相机出生同步场景。

修正完成记录：专项移动回归 24 条断言通过；完整 28 套自动化测试通过（88,826 条断言）；Windows PCK 导出为 295,720 字节，主场景 120 帧发布冒烟通过。下一检查点为人工确认新局、继续游戏与敌人贴身时均不再出现无输入滑动。

碰撞回归修正：永久 `collision_mask = 0` 方案已撤销；新增真实物理测试覆盖护盾接触拾取、敌方子弹接触伤害，以及普通敌人和 Boss 的路径阻挡。出生输入门禁与进入战斗时的相机同步保留。

碰撞回归验证：完整 28 套自动化测试通过（88,829 条断言）；Windows PCK 导出为 295,768 字节，主场景 120 帧发布冒烟通过。下一检查点为人工试玩护盾拾取、敌弹命中、普通敌人与 Boss 身体阻挡，并重新观察出生稳定性。

## 18. 2026-07-28 七次试玩反馈：出生坐标、掉落物与终极进化重做

本阶段先以“敌人进入场景树时就处于传送门坐标”修复偶发玩家瞬移，不改玩家碰撞层；随后补齐生存掉落、电弧运动语义、两套终极进化和世界空间战斗反馈。预计工程时间 **4–7 小时**，完成自动化、导出冒烟后进入人工试玩。

### Task 18.1：出生物理回归与敌人基础状态

- 新增敌人 `child_entered_tree` 坐标回归，确保碰撞体第一次参与物理计算前已放到传送门位置，杜绝从原点/玩家位置移动到传送门造成的瞬时碰撞恢复。
- 所有普通敌人与 Boss 基础移动速度统一提高 `10%`。
- 大型敌人头顶血条使用当前生命/最大生命比例绘制，并在每次伤害后刷新。

### Task 18.2：护盾、爱心与容量卡

- 护盾满值时拒绝拾取并让掉落物留在原地；初始护盾上限调整为 `20`。
- 普通敌人死亡时独立进行 `5%` 小爱心掉落判定；非满血玩家拾取恢复 `20` 点生命。
- 新增护盾容量卡：护盾上限 `+20` 并立即补满，纳入工程流派升级目录与快照恢复。

### Task 18.3：电弧绝对波速与数值再平衡

- 电弧扩散全程跟随玩家，以玩家实时位置为圆心。
- 波前改用固定像素/秒速度，扩散半径只改变持续时间，不再改变传播速度。
- 电弧基础伤害提高 `20%`，同时略降启动卡与伤害升级卡的成长倍率。

### Task 18.4：榴弹终极进化

- 保留火力终极进化的存档 ID，但将效果替换为榴弹：射速和弹速降至 `30%`，靠近敌人自动爆炸。
- 爆炸造成基础子弹伤害 `300%` 的小范围伤害，并提供独立的程序化爆炸特效和音效。

### Task 18.5：顶级刺客与冲刺可读性

- 保留机动终极进化已有冷却、距离和其他成长增益，移除冲刺黑色地刺。
- 每次冲刺结束获得 `3s` 的 `30%` 移速加成与半透明隐身；敌人与 Boss 暂停索敌，但已有弹幕仍可命中玩家。
- 冲刺路径生成紫色火焰，对踩入的敌人施加 `4s` 燃烧（每秒 `100%` 当前攻击力）和 `30%` 减速。
- 玩家脚下绘制宽约角色 `150%` 的冲刺蓄能条，冷却恢复与可用状态清晰可见。

### Task 18.6：验证、提交与试玩交接

- 以专项测试覆盖出生入树坐标、拾取条件、掉落概率、卡片恢复、固定电弧波速/跟随、榴弹爆炸、隐身索敌、燃烧减速、冲刺条和大型敌人血条。
- 完整回归、Windows PCK 导出与 120 帧冒烟通过后，聚焦提交并推送，发送邮件提醒，再直接启动当前工作树游戏。

### Task 18.7：无人机持续锁定燃烧与工程终极微调

- 单架无人机持续攻击同一目标满 `0.6s` 后施加一次 `4s` 燃烧；切换目标时重新计时，多架无人机独立记录锁定状态。
- 无人机初始攻击力提高 `20%`（`24 → 28.8`）。
- 雷网矩阵的无人机伤害倍率由 `2.0×` 调整为 `1.8×`，全屏电弧伤害按常态结果再降低 `30%`。

完成记录：出生回归在修复前稳定捕获敌人先于父节点原点入树、修复后通过；完整 28 套自动化测试通过（88,848 条断言），三流派 192 局固定种子经济门禁通过；Windows PCK 导出为 309,712 字节，主场景 120 帧发布冒烟通过。下一检查点为人工试玩出生稳定性、榴弹近敌爆炸手感、顶级刺客隐身/紫焰路径、无人机 0.6 秒燃烧以及世界空间冲刺条可读性。

## 19. 2026-07-29 八次试玩反馈：叠层灼烧、惯性弹道与玩家入场

本阶段收敛三条终极进化的战斗语义，并用不移动玩家物理实体的入场演出关闭出生瞬移风险。预计自动化实现与验证 **4–6 小时**，之后进入 **30–60 分钟**人工试玩。

### Task 19.1：工程模块与灼烧契约

- 灼烧每 0.2 秒新增一层、最多 5 层；每层每秒造成施加时基础攻击力 30% 的伤害，独立计时 4 秒。
- 灼烧使用独立伤害类型，保留命中音效但不触发受击晃动；电弧波固定在发射坐标。
- 新增“穿透棱镜”卡，只有获得无人机后才可出现；激光开启后贯穿同一直线上的敌人且单个目标每帧只结算一次。

### Task 19.2：机动模块

- 顶级刺客隐身缩短至 1.2 秒；发射主武器立即破隐。
- 失去隐身目标的敌人以稳定随机方向扩散运动，不原地停住；Boss 同样保持移动但仍停止主动索敌攻击。
- 冲刺仅留下紫焰路径，不产生地刺；所有地刺伤害不触发晃动；紫焰加入逐帧横向摆动。

### Task 19.3：火力、掉落与敌速

- 榴弹弹速降至常态 25%，射速降至常态 20%。
- 普通子弹和榴弹初速度统一为“玩家当前速度向量 + 枪口弹速向量”。
- 小爱心掉率改为 2.5%，大型敌人护盾掉落判定改为原概率的 70%；所有非大型普通敌人的基础速度固定为玩家初始速度的 105%。

### Task 19.4：玩家落地入场门控

- 玩家物理坐标与碰撞体从创建起固定在出生点，仅绘制层从屏幕上方落下。
- 落地播放程序化烟雾与冲击环；整个演出期间禁用玩家物理与首波准备。
- 演出结束信号触发首波准备，随后仍按原波次横幅进入战斗，保留输入松开门控和完整碰撞层。

### Task 19.5：验证与试玩交接

- 专项测试覆盖叠层/上限/计时、反馈抑制、穿透前置、随机扩散、破隐、惯性弹道、概率边界、敌速及入场门控。
- 全量回归、Windows PCK 导出与 120 帧冒烟通过后，更新进度、聚焦提交并推送。
- 发送邮件提醒并直接启动当前工作树游戏，进入人工试玩检查点。

完成记录：完整 29 套自动化测试连续两轮通过，最终为 88,875 条断言；穿透无人机 300 帧性能门禁保持每帧单次敌人注册表读取；Windows PCK 导出为 316,972 字节，主场景 120 帧发布冒烟通过。下一检查点为人工试玩五层灼烧、贯穿激光、隐身扩散、惯性榴弹与玩家落地演出。

## 20. 2026-07-29 九次试玩反馈：敌速分层、隐身碰撞与工程输出

本阶段进一步拉开近战追击与远程站位差异，并收敛三个终极模块的代价和输出上限。预计自动化实现与验证 **3–5 小时**，之后进入 **30–45 分钟**人工试玩。

### Task 20.1：敌速分层与榴弹弹速

- 拾荒者、突进者的基础速度固定为玩家初始速度的 `90%`；喷射者、神射手、投掷者固定为 `60%`。
- 大型敌人与 Boss 保持当前独立速度，不受本次普通敌人比例覆盖。
- 火力终极榴弹保留 `20%` 射速与惯性叠加，枪口弹速由常态 `25%` 调整为 `40%`。

### Task 20.2：顶级刺客碰撞与冲刺代价

- 同一敌人同时最多由三块地刺结算伤害；离开、销毁或敌人死亡时释放占用槽位。
- 顶级刺客隐身期间关闭玩家碰撞层与碰撞形状；开火、超时、暂停清理时恢复原碰撞契约。
- 终极进化将冲刺距离提高 `20%`，同时把冲刺冷却时间提高 `20%`；冲刺仍不生成地刺。

### Task 20.3：工程模块输出节奏

- 过载期间电弧不再获得伤害增幅，伤害倍率保持 `1.0×`；范围和频率等其他过载增益不变。
- 无人机激光瞄准方向以最大 `30°/s` 转向，未对准目标时不提前结算目标伤害。
- 所有灼烧层伤害提高 `100%`，即每层每秒由基础攻击力 `30%` 调整为 `60%`。

### Task 20.4：验证与试玩交接

- 专项测试覆盖敌速分类、榴弹 `40%` 弹速、三地刺上限、隐身碰撞恢复、冲刺距离/冷却、过载电弧、无人机转向和双倍灼烧。
- 完整回归、性能门禁、Windows PCK 导出与 120 帧冒烟通过后，更新进度并创建聚焦提交推送。
- 发送阶段完成邮件，关闭旧实例并直接启动当前工作树游戏，进入人工试玩检查点。

完成记录：Phase 20 专项测试 17 条断言通过；完整 30 套自动化测试在审查前后各通过一轮，最终为 88,892 条断言；四架穿透无人机 300 帧性能门禁约 266ms，仍保持每帧单次敌人注册表读取；Windows PCK 导出为 319,980 字节，主场景 120 帧发布冒烟通过。下一检查点为人工试玩敌速层次、无人机转向、三地刺上限、隐身穿透与破隐碰撞恢复。
