Task 2 snapshot review package
Base: .superpowers/sdd/task-2-baseline
Head: current working tree
--- DIFFS ---
warning: in the working copy of '.superpowers/sdd/task-2-baseline/scripts/actors/Enemy.gd', LF will be replaced by CRLF the next time Git touches it
warning: in the working copy of 'scripts/actors/Enemy.gd', LF will be replaced by CRLF the next time Git touches it
diff --git a/.superpowers/sdd/task-2-baseline/scripts/actors/Enemy.gd b/scripts/actors/Enemy.gd
index 63124c6..bf22c68 100644
--- a/.superpowers/sdd/task-2-baseline/scripts/actors/Enemy.gd
+++ b/scripts/actors/Enemy.gd
@@ -2,10 +2,11 @@ extends CharacterBody2D
 class_name Enemy
 
 signal died(enemy: Node, xp_value: int)
-signal hit
+signal hit(source: StringName)
 
 const ProjectileScript = preload("res://scripts/components/Projectile.gd")
 const HealthComponentScript = preload("res://scripts/components/HealthComponent.gd")
+const DamageTypes = preload("res://scripts/components/DamageTypes.gd")
 
 enum EnemyKind { SCRAPPER, DASHER, SPITTER, BRUISER }
 
@@ -135,12 +136,12 @@ func _draw() -> void:
 	draw_rect(Rect2(-5, -5, 4, 4), Color.BLACK)
 	draw_rect(Rect2(3, -5, 4, 4), Color.BLACK)
 
-func take_damage(amount: float) -> void:
+func take_damage(amount: float, source: StringName = DamageTypes.GENERIC) -> void:
 	if health == null:
 		return
 	flash_timer = 0.08
 	health.damage(amount)
-	hit.emit()
+	hit.emit(source)
 
 func _add_health(max_health: float) -> void:
 	health = HealthComponentScript.new()
warning: in the working copy of '.superpowers/sdd/task-2-baseline/scripts/components/Projectile.gd', LF will be replaced by CRLF the next time Git touches it
warning: in the working copy of 'scripts/components/Projectile.gd', LF will be replaced by CRLF the next time Git touches it
diff --git a/.superpowers/sdd/task-2-baseline/scripts/components/Projectile.gd b/scripts/components/Projectile.gd
index e72d644..c18a977 100644
--- a/.superpowers/sdd/task-2-baseline/scripts/components/Projectile.gd
+++ b/scripts/components/Projectile.gd
@@ -1,6 +1,8 @@
 extends Area2D
 class_name Projectile
 
+const DamageTypes = preload("res://scripts/components/DamageTypes.gd")
+
 var velocity: Vector2 = Vector2.ZERO
 var damage: float = 1.0
 var pierce: int = 0
@@ -10,6 +12,7 @@ var hit_bodies: Array[Node] = []
 var tint: Color = Color.CYAN
 var radius: float = 4.0
 var world_bounds: Rect2 = Rect2()
+var damage_source: StringName = DamageTypes.PROJECTILE
 
 func _ready() -> void:
 	monitoring = true
@@ -47,7 +50,7 @@ func _try_hit(node: Node) -> void:
 		return
 	hit_bodies.append(node)
 	if node.has_method("take_damage"):
-		node.take_damage(damage)
+		node.take_damage(damage, damage_source)
 	if pierce <= 0:
 		queue_free()
 	else:
warning: in the working copy of '.superpowers/sdd/task-2-baseline/scripts/components/SpikeTrap.gd', LF will be replaced by CRLF the next time Git touches it
warning: in the working copy of 'scripts/components/SpikeTrap.gd', LF will be replaced by CRLF the next time Git touches it
diff --git a/.superpowers/sdd/task-2-baseline/scripts/components/SpikeTrap.gd b/scripts/components/SpikeTrap.gd
index ae8fd80..eed63fe 100644
--- a/.superpowers/sdd/task-2-baseline/scripts/components/SpikeTrap.gd
+++ b/scripts/components/SpikeTrap.gd
@@ -1,6 +1,8 @@
 extends Area2D
 class_name SpikeTrap
 
+const DamageTypes = preload("res://scripts/components/DamageTypes.gd")
+
 var damage: float = 12.0
 var lifetime: float = 5.0
 var max_lifetime: float = 5.0
@@ -41,4 +43,4 @@ func _draw() -> void:
 func _damage_enemies() -> void:
 	for body in get_overlapping_bodies():
 		if body.is_in_group("enemies") and body.has_method("take_damage"):
-			body.take_damage(damage)
+			body.take_damage(damage, DamageTypes.SPIKE)
warning: in the working copy of '.superpowers/sdd/task-2-baseline/scripts/actors/Player.gd', LF will be replaced by CRLF the next time Git touches it
warning: in the working copy of 'scripts/actors/Player.gd', LF will be replaced by CRLF the next time Git touches it
diff --git a/.superpowers/sdd/task-2-baseline/scripts/actors/Player.gd b/scripts/actors/Player.gd
index f6aedde..262cf8d 100644
--- a/.superpowers/sdd/task-2-baseline/scripts/actors/Player.gd
+++ b/scripts/actors/Player.gd
@@ -5,13 +5,14 @@ signal health_changed(current: float, maximum: float)
 signal shield_changed(current: float, maximum: float)
 signal died
 signal fired(projectile: Node)
-signal laser_fired
+signal laser_active_changed(active: bool)
 
 const ProjectileScript = preload("res://scripts/components/Projectile.gd")
 const HealthComponentScript = preload("res://scripts/components/HealthComponent.gd")
 const LaserBeamScript = preload("res://scripts/components/LaserBeam.gd")
 const SpikeTrapScript = preload("res://scripts/components/SpikeTrap.gd")
 const ArcPulseVisualScript = preload("res://scripts/components/ArcPulseVisual.gd")
+const DamageTypes = preload("res://scripts/components/DamageTypes.gd")
 
 var move_speed: float = 235.0
 var pickup_radius: float = 92.0
@@ -37,7 +38,7 @@ var dash_melee_damage: float = 52.0
 var dash_melee_radius: float = 36.0
 
 var fire_timer: float = 0.0
-var laser_audio_timer: float = 0.0
+var laser_audio_active: bool = false
 var arc_timer: float = 0.0
 var dash_timer: float = 0.0
 var dash_cooldown_remaining: float = 0.0
@@ -103,7 +104,7 @@ func _draw() -> void:
 	if arc_pulse_level > 0:
 		draw_arc(Vector2.ZERO, 78.0 + arc_pulse_level * 16.0, 0.0, TAU, 48, Color(0.25, 1.0, 1.0, 0.18), 2.0)
 
-func take_damage(amount: float) -> void:
+func take_damage(amount: float, _source: StringName = DamageTypes.GENERIC) -> void:
 	var remaining := amount
 	if shield > 0.0:
 		var absorbed := minf(shield, remaining)
@@ -182,7 +183,7 @@ func _apply_dash_melee() -> void:
 			continue
 		if _is_enemy_in_dash_melee(node.global_position):
 			dash_hit_bodies.append(enemy)
-			enemy.take_damage(dash_melee_damage)
+			enemy.take_damage(dash_melee_damage, DamageTypes.DASH)
 
 func _is_enemy_in_dash_melee(enemy_position: Vector2) -> bool:
 	var to_enemy := enemy_position - global_position
@@ -228,17 +229,11 @@ func _update_drone_lasers(delta: float) -> void:
 			continue
 		assigned.append(target)
 		any_laser_active = true
-		target.take_damage(drone_damage * delta)
+		target.take_damage(drone_damage * delta, DamageTypes.LASER)
 		var beam := drone_lasers[index]
 		beam.visible = true
 		beam.setup(origin, target.global_position, Color(0.2, 1.0, 0.95), 4.0)
-	if any_laser_active:
-		laser_audio_timer -= delta
-		if laser_audio_timer <= 0.0:
-			laser_audio_timer = 0.28
-			laser_fired.emit()
-	else:
-		laser_audio_timer = 0.0
+	_set_laser_audio_active(any_laser_active)
 
 func _damage_enemies_on_laser(origin: Vector2, direction: Vector2, length: float, damage: float, width: float) -> void:
 	for enemy in get_tree().get_nodes_in_group("enemies"):
@@ -251,13 +246,13 @@ func _damage_enemies_on_laser(origin: Vector2, direction: Vector2, length: float
 			continue
 		var closest := origin + direction * along
 		if closest.distance_to(node.global_position) <= width:
-			enemy.take_damage(damage)
+			enemy.take_damage(damage, DamageTypes.LASER)
 
 func _emit_arc_pulse() -> void:
 	var radius := arc_radius + arc_pulse_level * 18.0
 	for enemy in get_tree().get_nodes_in_group("enemies"):
 		if global_position.distance_to(enemy.global_position) <= radius and enemy.has_method("take_damage"):
-			enemy.take_damage(arc_damage + arc_pulse_level * 8.0)
+			enemy.take_damage(arc_damage + arc_pulse_level * 8.0, DamageTypes.ARC)
 	var wave := ArcPulseVisualScript.new()
 	wave.global_position = global_position
 	wave.setup(radius)
@@ -323,12 +318,19 @@ func _sync_drone_lasers() -> void:
 		beam.queue_free()
 
 func _clear_drone_lasers() -> void:
+	_set_laser_audio_active(false)
 	for beam in drone_lasers:
 		if is_instance_valid(beam):
 			beam.queue_free()
 	drone_lasers.clear()
 	drone_targets.clear()
 
+func _set_laser_audio_active(active: bool) -> void:
+	if laser_audio_active == active:
+		return
+	laser_audio_active = active
+	laser_active_changed.emit(active)
+
 func _update_drone_positions() -> void:
 	if drone_visuals.is_empty():
 		return
warning: in the working copy of '.superpowers/sdd/task-2-baseline/scripts/systems/WaveDirector.gd', LF will be replaced by CRLF the next time Git touches it
warning: in the working copy of 'scripts/systems/WaveDirector.gd', LF will be replaced by CRLF the next time Git touches it
diff --git a/.superpowers/sdd/task-2-baseline/scripts/systems/WaveDirector.gd b/scripts/systems/WaveDirector.gd
index dcb9db1..f08b9e5 100644
--- a/.superpowers/sdd/task-2-baseline/scripts/systems/WaveDirector.gd
+++ b/scripts/systems/WaveDirector.gd
@@ -83,9 +83,9 @@ func _spawn_enemy(kind: int) -> void:
 	enemy_parent.add_child(enemy)
 	enemy.died.connect(_on_enemy_died)
 	if enemy.has_signal("hit"):
-		enemy.hit.connect(func() -> void:
+		enemy.hit.connect(func(source: StringName) -> void:
 			if get_parent().has_method("_on_enemy_hit"):
-				get_parent()._on_enemy_hit()
+				get_parent()._on_enemy_hit(source)
 		)
 
 func _on_enemy_died(enemy: Node, xp_value: int) -> void:
warning: in the working copy of '.superpowers/sdd/task-2-baseline/scripts/systems/AudioManager.gd', LF will be replaced by CRLF the next time Git touches it
warning: in the working copy of 'scripts/systems/AudioManager.gd', LF will be replaced by CRLF the next time Git touches it
diff --git a/.superpowers/sdd/task-2-baseline/scripts/systems/AudioManager.gd b/scripts/systems/AudioManager.gd
index 95020dc..a2ee71c 100644
--- a/.superpowers/sdd/task-2-baseline/scripts/systems/AudioManager.gd
+++ b/scripts/systems/AudioManager.gd
@@ -1,10 +1,23 @@
 extends Node
 class_name AudioManager
 
+const DamageTypes = preload("res://scripts/components/DamageTypes.gd")
+const HIT_COOLDOWN := 0.055
+
 var streams: Dictionary = {}
 var bgm_player: AudioStreamPlayer
 var bgm_volume_linear: float = 0.65
 var bgm_muted: bool = false
+var hit_stream_names: Dictionary = {
+	DamageTypes.GENERIC: "enemy_hit",
+	DamageTypes.PROJECTILE: "hit_projectile",
+	DamageTypes.LASER: "hit_laser",
+	DamageTypes.ARC: "hit_arc",
+	DamageTypes.DASH: "hit_dash",
+	DamageTypes.SPIKE: "hit_spike",
+}
+var hit_cooldowns: Dictionary = {}
+var laser_loop_player: AudioStreamPlayer
 
 func _ready() -> void:
 	process_mode = Node.PROCESS_MODE_ALWAYS
@@ -15,10 +28,38 @@ func _ready() -> void:
 	streams["upgrade"] = _make_tone(520.0, 0.18, 0.22, 0.45, 1.8)
 	streams["hit"] = _make_tone(180.0, 0.05, 0.18, 0.32)
 	streams["enemy_hit"] = _make_impact(0.075, 0.58, 820.0, 0.42)
+	streams["hit_projectile"] = _make_impact(0.055, 0.5, 980.0, 210.0)
+	streams["hit_laser"] = _make_harmonic_impact(0.1, 0.36, 720.0, 0.08)
+	streams["hit_arc"] = _make_harmonic_impact(0.12, 0.38, 430.0, 0.16)
+	streams["hit_dash"] = _make_impact(0.09, 0.62, 310.0, 95.0)
+	streams["hit_spike"] = _make_impact(0.07, 0.48, 1450.0, 360.0)
+	streams["laser_loop"] = _make_laser_loop()
 	streams["enemy_death"] = _make_impact(0.18, 0.72, 180.0, 0.28)
 	streams["start"] = _make_tone(360.0, 0.22, 0.18, 0.55, 2.2)
 	streams["victory"] = _make_tone(740.0, 0.35, 0.16, 0.55, 1.6)
 	streams["defeat"] = _make_tone(120.0, 0.45, 0.2, 0.55, 0.55)
+	laser_loop_player = AudioStreamPlayer.new()
+	laser_loop_player.stream = streams["laser_loop"]
+	laser_loop_player.volume_db = -18.0
+	add_child(laser_loop_player)
+
+func _process(delta: float) -> void:
+	for source in hit_cooldowns.keys():
+		hit_cooldowns[source] = maxf(0.0, float(hit_cooldowns[source]) - delta)
+
+func play_hit(source: StringName) -> bool:
+	var resolved: StringName = source if hit_stream_names.has(source) else DamageTypes.GENERIC
+	if float(hit_cooldowns.get(resolved, 0.0)) > 0.0:
+		return false
+	hit_cooldowns[resolved] = HIT_COOLDOWN
+	play(String(hit_stream_names.get(resolved, "enemy_hit")))
+	return true
+
+func set_laser_active(active: bool) -> void:
+	if active and not laser_loop_player.playing:
+		laser_loop_player.play()
+	elif not active and laser_loop_player.playing:
+		laser_loop_player.stop()
 
 func play(name: String) -> void:
 	if not streams.has(name):
@@ -135,3 +176,46 @@ func _make_impact(duration: float, volume: float, start_freq: float, end_freq: f
 	wav.stereo = false
 	wav.data = data
 	return wav
+
+func _make_harmonic_impact(duration: float, volume: float, frequency: float, noise_mix: float) -> AudioStreamWAV:
+	var sample_rate := 22050
+	var sample_count := int(duration * sample_rate)
+	var data := PackedByteArray()
+	data.resize(sample_count * 2)
+	var noise_seed := 24681
+	for i in range(sample_count):
+		noise_seed = int((1103515245 * noise_seed + 12345) & 0x7fffffff)
+		var t := float(i) / float(sample_rate)
+		var progress := float(i) / maxf(1.0, float(sample_count - 1))
+		var attack := minf(1.0, progress / 0.12)
+		var envelope := attack * pow(1.0 - progress, 1.8)
+		var harmonic := sin(TAU * frequency * t) + sin(TAU * frequency * 2.0 * t) * 0.36
+		var noise := ((float(noise_seed % 2000) / 1000.0) - 1.0) * noise_mix
+		data.encode_s16(i * 2, int(clampf((harmonic * 0.55 + noise) * envelope * volume, -1.0, 1.0) * 32767.0))
+	var wav := AudioStreamWAV.new()
+	wav.format = AudioStreamWAV.FORMAT_16_BITS
+	wav.mix_rate = sample_rate
+	wav.stereo = false
+	wav.data = data
+	return wav
+
+func _make_laser_loop() -> AudioStreamWAV:
+	var sample_rate := 22050
+	var duration := 1.0
+	var sample_count := int(duration * sample_rate)
+	var data := PackedByteArray()
+	data.resize(sample_count * 2)
+	for i in range(sample_count):
+		var t := float(i) / float(sample_rate)
+		var modulation := 0.82 + 0.18 * sin(TAU * 3.0 * t)
+		var harmonics := sin(TAU * 180.0 * t) * 0.55 + sin(TAU * 360.0 * t) * 0.28 + sin(TAU * 540.0 * t) * 0.12
+		data.encode_s16(i * 2, int(clampf(harmonics * modulation * 0.22, -1.0, 1.0) * 32767.0))
+	var wav := AudioStreamWAV.new()
+	wav.format = AudioStreamWAV.FORMAT_16_BITS
+	wav.mix_rate = sample_rate
+	wav.stereo = false
+	wav.loop_mode = AudioStreamWAV.LOOP_FORWARD
+	wav.loop_begin = 0
+	wav.loop_end = sample_count
+	wav.data = data
+	return wav
warning: in the working copy of '.superpowers/sdd/task-2-baseline/scripts/Main.gd', LF will be replaced by CRLF the next time Git touches it
warning: in the working copy of 'scripts/Main.gd', LF will be replaced by CRLF the next time Git touches it
diff --git a/.superpowers/sdd/task-2-baseline/scripts/Main.gd b/scripts/Main.gd
index ef0dda4..1a05108 100644
--- a/.superpowers/sdd/task-2-baseline/scripts/Main.gd
+++ b/scripts/Main.gd
@@ -155,7 +155,7 @@ func _start_run() -> void:
 		projectiles.add_child(projectile)
 		audio.play("shoot")
 	)
-	player.laser_fired.connect(func() -> void: audio.play("laser"))
+	player.laser_active_changed.connect(audio.set_laser_active)
 	player.health_changed.connect(ui.set_health)
 	player.shield_changed.connect(ui.set_shield)
 	player.died.connect(_on_player_died)
@@ -191,8 +191,8 @@ func _on_enemy_killed(_xp_value: int) -> void:
 	audio.play("enemy_death")
 	ui.set_run_stats(kill_count, elapsed_seconds)
 
-func _on_enemy_hit() -> void:
-	audio.play("enemy_hit")
+func _on_enemy_hit(source: StringName) -> void:
+	audio.play_hit(source)
 
 func _on_player_died() -> void:
 	_end_run(false)
warning: in the working copy of '.superpowers/sdd/task-2-baseline/scripts/tests/BalanceTest.gd', LF will be replaced by CRLF the next time Git touches it
warning: in the working copy of 'scripts/tests/BalanceTest.gd', LF will be replaced by CRLF the next time Git touches it
diff --git a/.superpowers/sdd/task-2-baseline/scripts/tests/BalanceTest.gd b/scripts/tests/BalanceTest.gd
index 206ccd7..9e592f3 100644
--- a/.superpowers/sdd/task-2-baseline/scripts/tests/BalanceTest.gd
+++ b/scripts/tests/BalanceTest.gd
@@ -13,10 +13,10 @@ func _initialize() -> void:
 	var enemy: Node = EnemyScript.new()
 	enemy.setup(EnemyScript.EnemyKind.SCRAPPER, 1, Node.new())
 	root.add_child(enemy)
-	var received_source: StringName = &""
-	enemy.hit.connect(func(source: StringName) -> void: received_source = source)
+	var received := {"source": &""}
+	enemy.hit.connect(func(source: StringName) -> void: received["source"] = source)
 	enemy.take_damage(1.0, DamageTypes.LASER)
-	if received_source != DamageTypes.LASER:
+	if received["source"] != DamageTypes.LASER:
 		_fail("enemy hit signal did not preserve the damage source.")
 		return
 
warning: in the working copy of 'scripts/components/DamageTypes.gd', LF will be replaced by CRLF the next time Git touches it
diff --git a/scripts/components/DamageTypes.gd b/scripts/components/DamageTypes.gd
new file mode 100644
index 0000000..8a56f16
--- /dev/null
+++ b/scripts/components/DamageTypes.gd
@@ -0,0 +1,10 @@
+extends RefCounted
+class_name DamageTypes
+
+const GENERIC: StringName = &"generic"
+const PROJECTILE: StringName = &"projectile"
+const LASER: StringName = &"laser"
+const ARC: StringName = &"arc"
+const DASH: StringName = &"dash"
+const SPIKE: StringName = &"spike"
+const ALL: Array[StringName] = [PROJECTILE, LASER, ARC, DASH, SPIKE]
