# B 环境透明度检查

2026-09-06，Pillow 只读检查源文件，Godot Vulkan 实际截图检查渲染结果。

2026-09-08：用户授权无缩放重排。原始图保留2172×724；四个已采用区域重排至1088×1088透明画布，RGBA字节逐区域完全相同。没有alpha重映射或重采样。下述统计仍指原始生成图，不是重排后的填充画布。

- 地面：1254×1254 RGB，不透明。
- 障碍共享图集：2172×724 RGBA，alpha 范围 0–255。非零像素中，248–255 共 574105 个，201–247 共 4984 个，128–200 共 2441 个，49–127 共 3173 个，1–48 共 15371 个。
- 四个主体内部抽样 alpha 为 253/254，不能将其错误描述为源文件全体255；运行时 `prop_alpha.gdshader` 将近不透明区归一化、抑制低透明边缘残留。
- 透明修订中曾产生 RGB 棋盘背景版本，该版被拒绝，没有导入项目。
- AtlasTexture 分区来源于 alpha≥128 的主体边界；启用 filter_clip 避免相邻素材串色。
- 实机截图未见白底或棋盘背景；温室挡住后方玩家是深度排序，不是角色 alpha 缺失。
- 不宣称所有边缘像素都完美，后续仍需动态缩放与运动中的边缘检查。
