# B 薄荷实验农场：首批运行时草稿

2026-09-06，分支 5分钟超载；状态 draft，不是 final。

## 2026-09-08：重排授权与校验修复

障碍源图的真实尺寸2172×724不满足现有manifest校验器的最小高度1024要求；地面manifest与台账验证通过，但障碍manifest验证失败。两次方形重生成实际得到1254×1254 RGB棋盘背景，均拒绝。没有修改校验器或虚填源尺寸。

用户已授权本地图像处理。已将原图四个运行时使用区域无缩放重排为1088×1088，逐区域RGBA字节比较完全一致，没有重采样或修改alpha。原图2172×724保存在docs/art/sources，manifest分别记录原始尺寸和重排画布尺寸。这只解决图集布局，不增加单个素材细节；所有视觉待办及draft状态保持不变。

当前验证：两个manifest及资产台账均通过；74个Python测试通过；36套Godot回归、98329个断言通过；MintFarm实机与前后遮挡截图重新渲染并检查。Python首次检查发现渲染脚本缺UID，Godot导入生成后复测通过。未修改验证器门槛。

重排复现（项目根目录执行Python，输出到另一个审查文件，勿覆盖原图）：

```python
from PIL import Image
from scripts.art.prepare_chibi_assets import pack_regions_without_scaling
source = Image.open("docs/art/sources/mint_farm_props_b_original_v1.png")
regions = [(20,298,511,277), (588,149,487,426),
           (1126,116,431,463), (1625,269,523,305)]
packed, targets = pack_regions_without_scaling(source, regions)
# packed为1088×1088；targets即ArenaObstacle中的四个区域。
```

## 本批成果

- 内置 imagegen 生成薄荷灰地面及花槽、温室、培养罐、管道四类独立障碍。生成提示词及修订要求见两个 mint_farm manifest。
- 提示词库检索没有匹配模板，使用自拟提示词，沿用用户明确批准的 B 配色，不开启新的风格选择。
- 地面以512像素单元重复绘制，取代旧霓虹线条及没有碰撞的假障碍装饰；保留 FloorGrid 兼容入口，地面绘制命令降为1。
- 四类障碍使用同一图集的区域，保持等比缩放。节点排序点移至前接触边，同时碰撞形状偏移回原位置，世界空间碰撞矩形不变。
- World、Enemies、ArenaLayout 启用 Y 排序；高障碍可遮住后方角色，前方角色在障碍之前绘制。
- 原始图保存在docs/art/sources，重排图集保存在runtime路径，不依赖生成缓存。重排保持区域像素不变，透明边缘归一化由运行时着色器处理。

## 验证证据

- 先增加独立障碍美术测试并确认旧实现失败，再接入。
- 全套 Godot 回归：36套、98304个断言通过（含既有测试，计数随随机障碍数量变化）。
- 随后增加碰撞矩形不变及等比缩放断言，单独运行 ArenaRuntimeTest：48个断言通过。
- 1280×720 真正 Main 场景截图：mint-farm-runtime-v1.png，固定地图种子20260831；渲染器使用独立测试存档，不覆盖用户正常进度。
- 四种实际尺寸及前后关系检查夹具：mint-farm-depth-v1.png。它不是随机地图截屏，只用于验证绘制关系。
- 透明度数据见 mint-farm-alpha-v1.md。
- 代码审查检查了共享纹理、受控图集区域、碰撞世界坐标、等比缩放、旧接口及测试存档隔离。

## 尚未通过的视觉项目

- 短花槽的实体底座留白过大；之后要调整底座表现或重新制作匹配足迹的花槽，不靠拉伸图片或偷偷缩碰撞解决。
- 温室后方玩家完全遮住的问题已在2026-09-08遮挡v2切片处理：仅遮挡玩家的障碍平滑淡化，玩家本体透明度不变。见mint-farm-occlusion-v2.md；仍待动态实玩进一步评估边缘触发范围。
- 地面重复规律仍明显，需要继续检查滚动时接缝与重复感。
- 尚未做密集战斗性能复测、完整场景遮挡测试和环境氛围层，不能标为 gameplay-approved。
- 本批不改 HUD，不发布 Release。来源/使用权状态 pending。

## 生成来源

- 地面：exec-d2d21ff4-2bf0-4bcf-80f4-518cff915d78.png。
- 障碍初稿：exec-f2027d62-349a-4605-a50c-72f554fc6ec0.png。
- 被拒绝的棋盘背景修订：exec-875faedb-11c1-4b9d-9421-4ca56da2784f.png。
- 采用的透明修订：exec-4c826e11-c589-4dc2-bff8-e885106cac8c.png。

美术流水线促使本批保留 draft 状态；增量实现将剩余底座及遮挡可读性明确保留为下一切片，不用测试通过替代视觉审批。
