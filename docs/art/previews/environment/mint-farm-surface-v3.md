# B 环境表面调整 v3

2026-09-08，5分钟超载。保持draft状态，沿用已选B风格，不重新生成位图。

## 本批调整

- 花槽裸露区域改为土壤色，增加三条浅种植沟；底座使用奶油色边沿与5像素小圆角。其余底座统一边沿处理，去掉原来的硬直细框。
- 底座仍完整覆盖既有矩形碰撞足迹，没有缩小碰撞、拉伸贴图或改变随机地图。
- 地面材质将原贴图与薄荷灰基色混合，保留52%原贴图对比；加入固定于场景的平滑低频明暗变化，倍率范围0.97–1.03，无时间动画。
- 保留一次地面绘制命令，不增加纹理、碰撞体或额外地图随机数。

## 验证

- 地面材质测试先失败，再实现并通过。
- 36套Godot回归、98326断言通过；74个Python测试通过。断言数量包含既有测试并受随机障碍数量影响。
- Vulkan渲染脚本通过：mint-farm-runtime-v3.png为Main场景；mint-farm-depth-v3.png为四类实尺寸底座检查夹具。
- 人工截图检查：花槽不再是空白绿板，砖缝对比更弱；角色轮廓清晰。v3夹具关闭淡化以便检查实体表面，游戏的遮挡淡化逻辑仍在且回归测试保留。
- 审查确认StyleBox只在障碍setup时创建，地面材质只在ready创建，shader没有时间项，碰撞及图集区域未变；git diff --check通过。

## 限制与下一步

本批是显示效果改进，未消除规则铺砖结构。未做移动中接缝/闪烁检查或新着色器的密集战斗性能复测，不能据一次绘制宣称GPU更快。下一步优先验证连续移动及密集战斗，再决定是否需要新地面变体。无Release发布。
